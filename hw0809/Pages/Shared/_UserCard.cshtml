@model dynamic

<div class="card" style="width:18rem;">
    <img src="@Model.AvatarUrl" class="card-img-top" alt="Avatar" />
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">@Model.Email</p>
    </div>
</div>